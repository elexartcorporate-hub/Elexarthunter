# PRD — Lead Hunter & Email Marketing Platform

## Original Problem Statement
Multi-tenant SaaS for lead generation and email marketing. Five modules: Dashboard, Hunter, Database, Email Marketing, Settings. Multi-tenant isolation via `tenant_id`. Multi-company hierarchy (sub-companies under one tenant with their own SMTP). Role-based access. Domain → company contact discovery with 7-step pipeline (Global Cache → Playwright Deep Crawl → Hunter.io → Merge → Dedupe → Confidence Scoring → Save).

User language: **Bahasa Indonesia** (respond in Indonesian).

## User Choices (verbatim)
- Auth: **JWT custom** (signup auto-creates tenant + Owner)
- Hunter.io: **MOCK** first, real API key swap-in via Settings
- Crawl engine: **Playwright real** (deep crawl)
- Email: **SMTP custom per sub-company** (or fallback tenant SMTP)
- Tracking: **Built-in** pixel + click-redirect
- Design: **Light SaaS aesthetic** (Indigo/Slate) — moved away from dark terminal theme per user request

## Architecture
- **Backend**: FastAPI + Motor + bcrypt + PyJWT + Playwright + smtplib (async)
- **Frontend**: React 19 + React Router 7 + Tailwind + Phosphor Icons + Sonner + Shadcn
- **DB**: MongoDB 7
- **Routing**: /api prefix; frontend SPA at /, /login, /register, /hunter, /database, /email, /settings
- **Infra**: Contabo VPS, Nginx, Let's Encrypt, Supervisor, auto-deploy bash script

## Core Requirements
1. Multi-tenant isolation via tenant_id from JWT
2. Sub-companies under one tenant, each with own SMTP + assigned users
3. Global hunter cache (cross-tenant) keyed by domain (<30d reuse)
4. 7-step Hunter pipeline with live UI feedback
5. Role-based access control (Owner / Admin / Staff + custom roles)
6. Email campaigns: pick sub-company SMTP, pick recipient source (my_leads / contacts / manual), tracking
7. Personal "My Leads" list per user

## Implemented (Feb 2026)

| Area | Status |
|---|---|
| JWT Auth (register / login / me / logout) | ✅ Tested |
| Multi-tenant isolation | ✅ Tested |
| Hunter single + bulk search (Playwright + mock Hunter.io) | ✅ Tested |
| Global hunter cache (30d) | ✅ Tested |
| Companies / Contacts CRUD | ✅ Tested |
| Sub-Companies CRUD + per-sub-company SMTP | ✅ Tested |
| Roles & Permissions (system + custom) | ✅ Tested |
| Categories & Locations | ✅ Tested |
| My Leads (private per-user list) | ✅ Tested |
| Light-mode SaaS UI redesign | ✅ Tested |
| Settings modal overlay (scroll fix) | ✅ Fixed Feb 2026 |
| Email Marketing 3-step Builder | ✅ NEW Feb 2026 |
| ↳ Sub-company SMTP picker | ✅ Tested |
| ↳ Recipient source: my_leads / contacts / manual | ✅ Tested |
| ↳ Live preview + draft + send-now | ✅ Tested |
| ↳ Campaign preview modal with recipients table | ✅ Tested |
| Open / click tracking | ✅ Tested |
| Dashboard KPIs + trends + recent | ✅ Tested |
| Contabo VPS deploy script (`deploy`) | ✅ |

## Recent Changes (Feb 2026)
- **WA LID Header Fix (Feb 2026)** — Active chat header di `WhatsApp.jsx` sekarang pakai 3-layer fallback: (1) `chats.find(x=>x.jid===activeJid).name` (di-backfill oleh wa-service dari push_name), (2) fallback ke push_name terbaru dari messages state, (3) baru fallback ke `🔒 LID#xxx`. Kombinasi dengan `sessionManager.js` line 485-486 yang auto-sync `wa_chats.name = pushName` di setiap incoming msg + backfill lazy di `index.js` line 141-158. Hasil: user melihat nama profil WhatsApp lawan bicara (bukan LID) selama minimal 1 incoming message sudah masuk.
- **WhatsApp CRM Pipeline MVP (Feb 2026)** — Rules engine: Cold (default new) → Hot on reply → Deal/Lost manual. Auto-recycle Lost/Cold → Hot ketika customer reply lagi. Stage counter naik tiap outbound follow-up (reset ke 0 saat reply). UI tab bar filter + per-chat status changer (6 opsi: Hot/Warm/Cold/Hold/Deal/Lost). Endpoints: `PATCH /api/whatsapp/accounts/{sid}/chats/{jid}/pipeline`, `GET /api/whatsapp/pipeline/counts`, `GET /api/whatsapp/pipeline/chats`.
- **WA Media 100MB via GridFS (Feb 2026)** — Lazy on-demand download ke MongoDB GridFS bucket `wa_media`. UI render image/video/audio/document (PDF, Docx). Endpoint `GET /api/whatsapp/accounts/{sid}/messages/{msgid}/media`.
- **Owner Unlimited WA Accounts (Feb 2026)** — Owner role bypass 3-account limit.
- **DevOps: Auto-kill stale port 8001 + `/api/version` diagnostic (Jul 1, 2026)**: Fix permanen untuk recurring "Method Not Allowed" bug yang muncul karena process supervisor zombie. (1) **`wa-setup.sh`** sekarang sebelum restart backend selalu: `fuser -k 8001/tcp` + `pkill -9 -f "uvicorn.*server:app"` + verify port kosong via `ss -tlnp` (force-kill PID kalau masih nyangkut). (2) **`GET /api/version`** endpoint baru — return git_sha, branch, commit message, server_file_mtime, key_endpoints_status (patch_whatsapp_account/assign_chat: ok|MISSING), dan full list registered WhatsApp routes. Sekali curl langsung confirm deploy berhasil tanpa tebak-tebakan. Auto-checked di akhir `wa-setup.sh`.

- **WhatsApp Team Inbox + Auto-Route (Feb 28, 2026)**: Multi-user WA inbox sharing dengan auto-routing chat masuk. (1) **Gear icon Setting** per koneksi → modal dengan dua field: **Nama/Label koneksi** (mis. "CS Bali") dan **Default Penerima Chat** dropdown user di tenant. Setelah disimpan, **semua chat masuk dari koneksi itu otomatis di-route ke user** yang dipilih (auto_assigned=True), termasuk backfill chat existing yang belum di-assign. Backend: `wa_accounts.default_assigned_user_id` + auto-fill di `GET /chats` endpoint untuk chat baru. (2) **Tabs di account list**: "Koneksi Saya" (own) vs "Inbox Tim" (chats assigned dari Admin) — emerald vs indigo. Account card menampilkan badge "Auto → Milla" supaya pemilik tahu chat masuk dialihkan ke siapa. (3) **Per-chat manual assign** masih tersedia via tombol UserPlus untuk override default (Owner/Admin/account-owner). Backend collection `wa_chat_assignments` `{session_id, jid, tenant_id, assigned_user_id, assigned_by, assigned_at, auto_assigned}`. (4) **RBAC pengirim**: user dengan assignment bisa kirim text & media pakai koneksi pemilik untuk chat itu; non-assigned chat tetap 403. (5) **Filter chat list**: user di Inbox Tim hanya lihat chat yang di-assign ke dirinya; Owner/Admin lihat semua dengan badge → `assigned_user_name`. (6) **Endpoints**: `PATCH /api/whatsapp/accounts/{sid}` (label + default_assigned_user_id), `POST/DELETE /api/whatsapp/accounts/{sid}/chats/{jid}/assign` (manual assign/unassign). Cleanup assignment otomatis saat akun dihapus. **Tested e2e via curl**: 8/8 alur sukses termasuk auto-assign backfill + auto-assign chat baru + unset default + RBAC enforced 5/5.

## Recent Changes (Feb 2026)
- **Templates UX overhaul (Phase 3, Feb 2026)**: Added `body_type` field (`html`|`plain`) to templates with toggle in UI, integrated **ReactQuill** WYSIWYG editor for HTML mode. Built template **attachment system** — files stored as base64 in MongoDB (`template_attachments` collection), max 8MB/file and 20MB/template. Endpoints: `POST /api/templates/{tid}/attachments` (multipart upload), `DELETE /api/templates/{tid}/attachments/{att_id}`, `GET /api/templates/{tid}/attachments/{att_id}/download`. Duplicate cascades attachments; delete cascades cleanup. Email sending in all paths (bulk, single, outreach task, scheduled worker) now auto-loads template `body_type` and `attachments` and sends with proper MIME `multipart/mixed` + `multipart/alternative` structure.
- **Anti-spam email service rewrite (Feb 2026)**: `send_smtp_email` now sets `Message-ID` (with sender's domain), `Date`, `MIME-Version`, `X-Mailer`, `Reply-To`, `List-Unsubscribe` + `List-Unsubscribe-Post: One-Click` headers. HTML bodies are wrapped with proper `<!DOCTYPE html><html><head>…</head><body>…</body></html>` if not already wrapped. Plain-text fallback (`multipart/alternative`) is always generated from HTML. Attachments use `multipart/mixed` with base64 transfer encoding. Tracking pixel only injected for HTML body_type (plain text doesn't get `<img>` tags).
- **Inbox Module Phase 2 (Feb 2026)**: Folder support (INBOX/Sent/Trash) with dynamic resolution via IMAP SPECIAL-USE flags + common-name fallback. Endpoints: `GET /api/inbox/{sc_id}?folder=…&limit=20`, `GET /api/inbox/{sc_id}/message/{uid}` (returns full body + auto-marks Seen), `POST /api/inbox/{sc_id}/mark`, `POST /api/inbox/{sc_id}/reply` (proper `In-Reply-To` + `References` threading + APPEND to Sent folder).
- **Settings modal overflow fix**: ModalShell rewritten to use `overflow-y-auto` on outer fixed overlay + `min-h-full flex items-start` wrapper. Modal headers/footers reachable at all viewport sizes including 1366x768.

## Prioritized Backlog

### Recently Completed (Feb 2026)
- **LinkedIn Prospect Module — Phase 2** (Feb 24, 2026) — 5 fitur sekaligus build atas MVP. **(1) Company Search Aggregator**: backend `company_search.py` scrape DuckDuckGo HTML → fallback Bing HTML kalau DDG di-block. Endpoint `POST /api/linkedin/search-companies {keyword, country, limit}` — returns 20 results dengan `{company_name, website, domain, snippet}`. Skip social/directory domains (Wikipedia, Booking, TripAdvisor, dll). Frontend SearchModal di header — input keyword "Hotel Bali" + country → tampil list kartu hasil + tombol "Add to Today" per row (langsung create LI prospect). Catatan: di pod (datacenter IP) DDG+Bing return Cloudflare/captcha, di VPS production akan jalan normal. **(2) Drag-and-Drop Kanban**: native HTML5 drag-drop (no library) — drag card antar stage column → onDrop trigger PATCH status → toast "Company → New Stage". Visual feedback: ring biru di drop zone, opacity 50% di card sedang di-drag. **(3) Table View**: kolom Company, DM, Title, Industry, City, Score, Status, Updated — clickable row buka detail. **(4) Advanced Filters**: input search `Filter company / DM / city` + dropdown `All stages` filter by pipeline status. Filter applies di all views (List/Kanban/Table). **(5) KPI Bar Chart**: recharts BarChart distribusi prospects per stage dengan color-coding tone (green=won, red=lost, amber=pending, purple=meeting, dll). **(6) Reminder Notifications**: endpoint `GET /api/linkedin/reminders` return 3/7/14 day buckets dari prospects yang `status=connect_sent` & `days_waiting >= N`. Frontend: amber Bell icon `[totalReminders] reminders` di header kalau ada — klik switch ke kanban view. Plus per-card Bell icon + "⏳ Xd" indicator di card list/kanban kalau >= 3 days.
- **LinkedIn Prospect Module (Phase 1 MVP)** (Feb 24, 2026) — Major new feature. Sales daily workflow untuk LinkedIn prospecting (manual, NEVER login ke LinkedIn, NEVER kirim invitation automation — full safety policy). **Backend** `/app/backend/linkedin_service.py`: LLM integration via Emergent key, Claude Sonnet 4.5 (claude-sonnet-4-5-20250929) untuk deep company research → structured JSON (summary, category, products[], services[], size_estimate, opportunity[], lead_score 0-100), Gemini 3 Flash (gemini-3-flash-preview) untuk message generation 4 jenis (connection_note <=280 chars, ice_breaker, first_message, follow_up) — auto-detect Bahasa Indonesia/English based on country. Endpoints di `server.py`: `GET /api/linkedin/dashboard?date=` (target/completed/remaining + today's prospects), `GET /api/linkedin/calendar?year=&month=` (counts per day), CRUD `/api/linkedin/prospects`, `POST /api/linkedin/prospects/{pid}/research`, `POST /api/linkedin/prospects/{pid}/messages/generate {kind, custom_hint}`, `GET /api/linkedin/kpi?date_from=&date_to=` (counters + rates: acceptance, conversation, meeting, deal). Settings: `PATCH /api/me/linkedin-target` + `PATCH /api/team/{uid}/linkedin-target` — field `linkedin_daily_target` TERPISAH dari email `daily_target`. Pipeline 12 stages: added → researched → dm_added → ready → connect_sent → accepted → conversation → follow_up → meeting → quotation → won/lost. RBAC: Owner sees all in tenant, others see own only. Auto-timeline events on status transition + research + DM update. **Frontend** `/app/frontend/src/pages/LinkedInProspect.jsx`: menu baru "LinkedIn" di sidebar (LinkedinLogo icon, perm:'prospects'), daily dashboard 4-card (Target/Completed/Remaining/Acceptance Rate), date picker, **List view** + **Kanban view** dengan drag-style swimlanes per stage, AddModal (company name + website + linkedin URL + industry + country + city), DetailDrawer dengan: Company info, AI Research card (Run/Re-run button → Claude generates structured), Decision Maker form (full name, title, dept, LinkedIn URL, email, phone, notes), AI Personalization grid (4 buttons Generate per kind → Gemini, Copy to clipboard), Pipeline Action buttons (kontekstual per status: Mark Ready / Open LinkedIn + Copy Note + Mark Connect Sent / Mark Accepted / Mark Message Sent / Mark Meeting/Won/Lost), waiting days counter, Timeline. **Self-tested e2e via curl**: target setting works (15), dashboard counters correct, prospect CRUD ok, Claude research returns valid JSON with lead_score=45 + category="Hotel & Accommodation Services", Gemini connection note Bahasa Indonesia generated correctly, KPI aggregation working, delete cleanup ok. Smoke screenshot verified.
- **Lanjutkan Button — Client-Side Fallback** (Feb 24, 2026) — Bug fix UTAMA. User di VPS (Elexart Corp) klik "Lanjutkan (5 prospect → tugas baru)" → "Backend belum punya endpoint" karena backend VPS belum di-deploy ulang. Fix definitive: `recoverOrphans()` di ProspectsCalendar sekarang **TRY new endpoint first → CATCH 404 generic Not Found → FALLBACK** ke client-side flow yang pakai endpoint LAMA: (1) `POST /api/tasks {date, name}` (sudah ada dari awal), (2) loop `POST /api/tasks/{tid}/prospects/{pid}` untuk tiap orphan (sudah ada juga). Hasil: **bekerja di backend lama maupun baru, tanpa user perlu deploy apa-apa**. Toast info "Menggunakan mode fallback (backend lama)" supaya user tahu. Self-tested e2e: create task + attach 3 prospects → 200 OK, all in DB, prospect_ids array benar. Lint clean.
- **Per-Prospect Email Status Badge + Better 404 Message** (Feb 24, 2026) — Bug fix + UX. (1) `recoverOrphans()` di ProspectsCalendar sekarang detect generic "Not Found" 404 dan tampilkan toast actionable: "Backend di VPS belum punya endpoint recover-orphans. Jalankan: cd /var/www/hunter.elexart.com && bash wa-setup.sh" — bukan cuma "Not Found" yang membingungkan. (2) UX requested: "kalau semua sudah di email baru prospect clear dan info di schedule time" — tiap prospect di "PROSPECTS DITAMBAH" list sekarang punya status badge: <Sent HH:MM> (success/green) kalau email sudah sent, <Scheduled DD Mmm HH:MM> (purple) kalau dijadwalkan, atau <Belum email> (neutral) kalau belum. Status di-derive dari `sent_emails` + `scheduled_emails` di `/api/prospects/calendar/day/{date}` response — index by prospect_id.
- **WA Doctor diagnostic + Backend spawn-error diagnostic in wa-setup.sh** (Feb 24, 2026) — Bug fix follow-up: setelah `wa-setup.sh` jalan, user di VPS masih lihat WA error karena `hunter-backend: ERROR (spawn error)`. Solusi: (1) `wa-setup.sh` sekarang deep-diagnostic kalau backend gagal spawn — dump supervisor config + check command path executable + last 50 lines err log + last 20 lines out log + suggest venv rebuild kalau path pakai `/venv/`. (2) NEW: `/app/wa-doctor.sh` — pure read-only diagnostic. Print 12 section: git status, supervisor status (semua program), backend .env (masked), supervisor configs, error logs, port checks (8001/3002/27017), health endpoint tests, Python venv check (httpx/fastapi versions), Node versions, code freshness check. User tinggal jalankan `bash wa-doctor.sh` di VPS, screenshot output, kirim ke main agent untuk exact diagnosis. (3) Frontend banner: tambah section "Kalau wa-setup.sh udah jalan tapi MASIH error" dengan command `bash wa-doctor.sh` + common fix venv rebuild commands.
- **Orphan Prospect Recovery** (Feb 24, 2026) — Bug fix: user add 5 prospects lalu terputus tanpa selesai task → tombol "Lanjutkan" hilang karena prospects tidak ter-link ke task manapun (orphan). Solusi: (1) endpoint baru `POST /api/tasks/recover-orphans/{date}` yang cari prospect user `created_at` pada tanggal X yang TIDAK terhubung ke task apapun, lalu auto-create draft task dengan `name='Recovered <date>'`, `recovered_from_orphans=true`, dan attach semua orphan ke task itu. Idempotent: panggil kedua kali → 404. (2) Frontend `ProspectsCalendar.jsx` DayDrawer: card amber `[data-testid='orphan-recover-card']` muncul OTOMATIS saat `orphanProspects.length > 0 && !hasActionableTask` — tampilkan jumlah prospect + tombol "Lanjutkan (N prospect → tugas baru)" yang trigger recovery + langsung navigate ke task continuation. Pytest 5/5 pass (auth, invalid date, no orphans 404, happy path, idempotency, RBAC isolation Owner vs Staff).
- **WA One-Command Setup (`wa-setup.sh`)** (Feb 24, 2026) — Script all-in-one + integrasi `deploy.sh`. Auto-detect APP_DIR, generate/reuse secret, write supervisor config dengan full paths, yarn install, restart wa-service + backend, verify health. Idempotent. `deploy.sh` di-update untuk auto-call wa-setup.sh kalau wa-service belum di supervisor.
- **WA Health Diagnostic Endpoint + Banner** (Feb 23, 2026) — Bug fix: user di VPS report "Not Found" saat klik Add WhatsApp tanpa info kenapa. Solusi: (1) endpoint baru `GET /api/whatsapp/health` (PUBLIC, no auth) yang ping wa-service via httpx 5s timeout & return `{backend, wa_service_url, wa_service, wa_service_detail}`. (2) Frontend banner `[data-testid='wa-health-banner']` render otomatis kalau wa_service !== "ok" — tampilkan diagnosis + expandable section "📖 Cara fix". (3) Error handling di handleAdd: 404 → "Backend belum mendukung WhatsApp" + 503 → "WA Service tidak jalan" — keduanya trigger loadHealth() ulang. (4) Refresh button juga re-run health.
- **WhatsApp Phase 2 — Media + Groups + Realtime + Persistent Connection** (Feb 23, 2026) — Build atas Phase 1. (1) **Persistent connection** (tidak terputus selama-lamanya): exponential backoff reconnect (3s→5s→8s→13s→21s→max 60s + jitter), keep-alive `sock.sendPresenceUpdate('available')` setiap 60s, `keepAliveIntervalMs: 25_000` di Baileys socket options, MongoDB auth state membuat session resume otomatis saat sidecar restart. (2) **Send media**: image/video/document/audio (max 50MB) via base64 upload — endpoint `POST /api/whatsapp/accounts/{sid}/chats/{jid}/media`. Frontend: paperclip button + hidden file input + chunked base64 encoding (avoid stack overflow). Media types render dengan badge label (🖼️/🎬/🎵/📎) + file size + caption. (3) **Group chat**: `is_group` flag + `group_size`/`group_subject` fetched via `groupMetadata()` on chats.upsert. UI: emerald UsersThree icon + size '(N)' badge. Endpoint baru `GET /api/whatsapp/accounts/{sid}/groups`. (4) **Realtime via delta polling**: backend chats & messages endpoints support `?since_ts=<ISO>` — return hanya record yang lebih baru. Frontend polling 4s chats + 2.5s messages dengan since_ts incremental merge → tiny payloads + smooth UX (mendekati realtime tanpa kompleksitas WebSocket/SSE). (5) **Defensive cleanup** di DELETE: hapus `wa_accounts` + `wa_chats` + `wa_messages` + `wa_contacts` + auth state. **Testing**: pytest 17/17 pass (9 phase2 + 8 bounce regression) + Playwright 100% pass pada 6 flow E2E.
- **WhatsApp Multi-Account (Baileys) — Phase 1** (Feb 23, 2026) — Fitur baru menu **WhatsApp** di sidebar. Tiap user bisa add max **3 akun WA** via QR scan. **Owner** bisa melihat (read-only / sales monitoring) semua chat user di tenant — tapi tidak bisa kirim pesan dari WA milik orang lain (403). Persistent across restarts: auth state Baileys disimpan di MongoDB (`wa_auth_creds` + `wa_auth_keys` collections via custom adapter di `wa-service/src/mongoAuthState.js`). Arsitektur: Node.js sidecar service (`/app/wa-service/`, port 3002, Express + Baileys 6.17.16) + FastAPI proxy endpoints + React UI 3-panel (account list + chat list + messages). Auto-reconnect semua sesi saat sidecar start. Endpoint: `GET/POST /api/whatsapp/accounts`, `GET /api/whatsapp/accounts/{sid}/status`, `DELETE /api/whatsapp/accounts/{sid}`, `GET /api/whatsapp/accounts/{sid}/chats`, `GET/POST /api/whatsapp/accounts/{sid}/chats/{jid}/messages`. Supervisor config: `/etc/supervisor/conf.d/supervisord_wa_service.conf` (pod) + `/app/etc/supervisor.wa-service.conf` (VPS template). `deploy.sh` di-update untuk yarn install + restart wa-service. Test agent: backend 100% (9/9 tests pass), frontend 95% (semua flow verified via Playwright — QR muncul ~1-2s, account card, delete, EmptyState, RBAC enforced).
- **Mailer-Daemon Auto-Bounce Detector** (Feb 23, 2026) — Inbox IMAP sync sekarang auto-detect email dari Mailer-Daemon/Postmaster (regex on From/Subject), fetch full body, parse DSN `Final-Recipient` + `Status` + `Diagnostic-Code` (RFC 3464), dan otomatis: (1) update `email_sends.status=bounce`, (2) pull bounced email dari `prospects.emails[]`, (3) upsert ke `bounced_emails`. Hard bounce only (5.x.x) — soft bounce (4.x.x) di-skip. New helper `_mark_bounced_by_email(tenant_id, email, error)` di `server.py`. New endpoint `POST /api/inbox/{sc_id}/rescan-bounces` untuk scan email Mailer-Daemon yang sudah ada di inbox cache (sebelum fitur ini aktif). Frontend Inbox page: tombol "Rescan Bounces" + toast notification saat sync menemukan bounce. Pytest regression suite: `backend/tests/test_bounce_parser.py` (8 tests all pass).
- **Manual Add Prospect + Existing Picker** (Feb 22, 2026) — (1) Tombol "+ Add Manual" di header Prospect List → buka modal form (Nama Perusahaan*, Website, Email*, Nama Kontak, Jabatan, Industri, Kota, Negara, Telepon, Notes) + checkbox "Langsung masuk ke tugas aktif" yang otomatis attach prospect baru ke active task setelah save. (2) Komponen `ExistingProspectPicker` di tab "2 · Add Prospect" (hanya tampil jika ada active task) → expandable section "Pakai prospect yang sudah tersimpan" → debounced search-as-you-type 300ms terhadap `/api/prospects?q=...` → list 50 hasil dengan badge status → tombol Add per row langsung attach ke task aktif (otomatis exclude prospect yang sudah di task). Backend tidak perlu endpoint baru — pakai POST /api/prospects + POST /api/tasks/{tid}/prospects/{pid} yang sudah ada.
- **Source Column di Hasil Search** (Feb 22, 2026) — Tabel Emails Found punya kolom Source: Playwright / Hunter.io / Playwright + Hunter.io (cross-validated hijau) / Alias / Playwright (ext).
- **Hunter.io Pagination Fix** (Feb 22, 2026) — Pagination `PAGE_SIZE=100`, loop `offset` sampai habis atau `HUNTER_MAX_RESULTS=500`. SMTP probe di-skip untuk Hunter-sourced (Hunter sudah verifikasi internal). Tested: murata.com → 309 contacts (sebelumnya 4).
- **Daily Target Auto-Sync** (Feb 22, 2026) — Saat user/owner update `daily_target` di Settings, backend otomatis update semua task draft/ready milik user. Frontend Prospects: tab dan progress card handle target=0 (mode bebas).
- **LinkedIn Identity per Company Profile + Test Generate** (Feb 26, 2026) — Owner/Admin sekarang bisa set LinkedIn identity (profile URL, profile name, signature, default connection template) per sub_company di Settings → Companies. AI Gemini Flash 3 menggunakan identitas ini saat generate connection note untuk user yang ditugaskan ke company tersebut. Endpoints: `PATCH/GET /api/companies/{sc_id}/linkedin-settings`, `POST /api/companies/{sc_id}/linkedin-settings/test-generate` (preview AI message pakai dummy prospect tanpa save ke DB — 4 kind: connection_note/ice_breaker/first_message/follow_up), `GET /api/linkedin/sender-context`. LinkedIn Prospect page menampilkan badge "Sending as: [Profile Name] · [Company]" di atas dashboard; bila kosong, banner warning Owner/Admin pointing ke Settings.
- **Inline Image Auto-Resize/Optimize** (Feb 22, 2026) — Backend `_optimize_inline_image()` (Pillow): downsize ke max 800px sisi terpanjang + recompress (JPEG quality 85, PNG dengan optimize=True, alpha preserved jika ada). EXIF orientation respected (foto HP otomatis tegak). GIF di-skip (preserve animation). Result: 3.2MB JPEG → 169KB (-94.8%). Safety fallback: tidak menggantikan kalau hasil lebih besar tanpa resize. Frontend toast tampilkan reduction percentage saat sukses.
- **Inline Image Upload in Email Body (Signature)** (Feb 22, 2026) — User dapat upload gambar (max 5MB: png/jpg/gif/webp) langsung dari toolbar Quill editor di OutreachModal, EmailStep, dan Templates page. Backend endpoint: `POST /api/uploads/inline-image` simpan base64 di MongoDB `inline_images`, return URL publik (`/api/inline-images/{id}.{ext}`). Saat email dikirim, helper `_extract_inline_images_for_send()` parse `<img src>` lalu konversi ke `cid:img-{id}` + attach sebagai MIME multipart/related inline parts → gambar otomatis tampil di Gmail/Outlook/Apple Mail tanpa download manual (RFC2392 standard).
- **Dashboard Team Performance (P1)** (Feb 21, 2026) — `/dashboard/daily` now returns `team_breakdown` for Owner/Admin/Manager: per-user leaderboard with prospects_today, quota_pct progress bar, emails sent today, total prospects, interested, customers won, replies. Sorted by prospects_today desc with trophy icon on #1. Staff sees own stats only (empty leaderboard). Highlighted "you" row in indigo. Quota bar color: red <50%, amber <100%, emerald 100%.
- **Per-user Templates scope** (Feb 21, 2026) — `/templates` endpoint + update/delete/duplicate/attachments now filter by `created_by == user.id`. Each user only sees & manages templates they personally created.
- **Per-user Sub-companies scope** (Feb 21, 2026) — `/sub-companies` endpoint now filters by user's `sub_company_ids`. Staff users only see their assigned company's SMTP. Owner/Admin (manage_company perm) still see all in tenant.
- **Outreach Modal refresh button** (Feb 21, 2026) — Added explicit "Refresh" button next to Template dropdown + auto-refresh on window focus. Both OutreachModal (Prospects bulk) and EmailStep (Tab 3 task submit) include this. Warning banner shown when user has no templates yet.
- Category-based dynamic alias injection (backend + Settings UI) — DONE
- Required category dropdown before domain search on Add Prospect tab — DONE
- Added Today sidebar isolated per active task (no stale prospects from submitted tasks) — DONE
- OutreachModal: Mode toggle Kirim Sekarang vs Jadwalkan with date+time picker — DONE
- Backend defensive validation: `scheduled_at` past-date rejected with 400 — DONE
- **Hunter verifier polling for HTTP 202 pending status** — DONE (3 polls × 2s)
- **Website-sourced emails auto-marked `verified`** (published on official site = trustworthy) — DONE
- **Alias emails get accurate status** based on Hunter.io verifier (deliverable→verified, undeliverable→invalid, risky→risky, unknown→score-based) — DONE
- **Refresh button** on Prospects search result to bypass 30-day cache & re-verify — DONE

### P1
- Real Hunter.io API integration (swap mock when user supplies key in Settings → Hunter.io API) — DONE (real API + Playwright deep crawl)
- Email body WYSIWYG (currently raw HTML textarea — works fine but plain) — DONE (ReactQuill)
- Audit log untuk aksi user (login, send email, add prospect)
- AES encryption untuk SMTP/IMAP password di MongoDB
- Lead Export to CSV (My Leads + Master Database)
- Per-user statistics on Dashboard (leads saved by each team member) — DONE (Feb 21, 2026 — Team Performance leaderboard)

### P2
- Email templates library + `{{variable}}` substitution per recipient — DONE
- Unsubscribe link auto-injection + suppression list — DONE
- Forgot-password flow
- Webhook receiver for SMTP bounces
- Custom tracking domains
- WebSocket streaming for Hunter pipeline (currently 2s polling)
- Refactor backend/server.py (>3600 lines) into routers (auth, hunter, prospects, email, inbox)

## Auth credentials
See `/app/memory/test_credentials.md`. Active demo: `demo@test.com / demo1234`.

## Deployment Workflow
After code changes verified: user clicks "Save to GitHub" in Emergent UI, then SSHs into Contabo VPS and runs `deploy` to pull & restart.
